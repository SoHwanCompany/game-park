# /push - 변경사항 푸시

현재 브랜치의 커밋을 리모트에 푸시합니다.

## 실행

1. 현재 브랜치 확인
2. 푸시되지 않은 커밋 확인
3. 리모트 브랜치 존재 여부 확인
4. 푸시 실행:
   - 리모트 브랜치 없으면: `git push -u origin <branch>`
   - 있으면: `git push`

## 주의사항

- `main` 또는 `develop` 브랜치에서는 경고
- force push 금지 (사용자가 명시적으로 요청하지 않는 한)
